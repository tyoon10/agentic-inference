# Visualization
matplotlib>=3.8
numpy>=1.24

# Agent loop (01), hybrid router (02), news aggregator (03)
# All use OpenAI-compatible endpoints (NVIDIA API, NIM, Mistral API)
openai>=1.0

# News aggregator (03) — no extra deps (uses stdlib urllib + xml.etree)
